#!/usr/bin/env bash
set -e

# bin/detect <build-dir>
BUILD_DIR=${1:-.}

# Detect Bun projects by presence of package.json or bun.lock
if [ -f "$BUILD_DIR/package.json" ] || [ -f "$BUILD_DIR/bun.lock" ]; then
  echo "bun-custom"
  exit 0
fi

exit 1


